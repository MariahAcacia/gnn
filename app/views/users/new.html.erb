<%= render template: 'shared/navbar' %>

<div class="row justify-content-center signup-form-header">
  <h1>Sign Up As New User</h1>
</div>

<%= render partial: 'form', locals: { current_user: @user } %>
