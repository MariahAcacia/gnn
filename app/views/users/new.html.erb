<div class="row">
  <div class="col text-right">
    <%= link_to "Back to Home", root_path %>
  </div>
</div>
<div class="row justify-content-center">
  <h1>Sign Up As New User</h1>
</div>

<%= render partial: 'form', locals: { current_user: @user } %>
