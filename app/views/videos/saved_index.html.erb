<%= render template: "shared/navbar" %>

<div class="row">
  <div class="col-sm-10 offset-sm-1">
    <h1>Your Saved Video Links:</h1>
  </div>
</div>

<div class="row">
  <div class="col-sm-10 offset-sm-1">
    <div class="row">
      <% @videos.each do |x| %>
        <%= render partial: 'shared/video_box', locals: { article: x } %> 
      <% end %>
    </div>
  </div>
</div>
