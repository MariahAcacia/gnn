<%= render template: 'shared/navbar' %>

<div class="row">
  <div class="col-sm-10 offset-sm-1">
    <h1>Your Saved Giving Links:</h1>
  </div>
</div>

<div class="row">
  <div class="col-sm-10 offset-sm-1">
    <% @givings.each do |x| %>
      <%= render template: "shared/giving_box", locals: { article: x } %> 
    <% end %>
  </div>
</div>
